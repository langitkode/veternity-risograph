# Cloudflare Pages — Build Configuration
#
# In the Cloudflare Pages dashboard, set:
#   Framework preset:  Astro
#   Build command:     npm run build
#   Build output dir:  dist
#   Node.js version:   20 (set via .node-version file)
#
# No wrangler.toml required for static Pages deployment.
# Just connect the WEB/ subdirectory (or repo root) to Cloudflare Pages.
